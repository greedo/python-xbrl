language: python
python:
    - "2.6"
    - "2.7"

before_install: sudo apt-get install libxslt1-dev libxml2

# command to install dependencies
install:
    - pip install -r requirements.txt

# command to run tests
script: py.test --tb=line -vs
